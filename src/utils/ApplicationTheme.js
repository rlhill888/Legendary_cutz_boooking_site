import { primaryColor, secondaryColor, thirdColor } from "./colors"
import { primaryfont } from "./typography"

export const defaultTheme = {

    primaryColor: primaryColor.main,
    primaryColorActive: primaryColor.bright,
    primaryColorDisabled: primaryColor.dark,

    secondaryColor: secondaryColor.main,
    secondaryColorActive: secondaryColor.bright,
    secondaryColorDisabled: secondaryColor.dark,

    thirdColor: thirdColor.main,
    thirdColorActive: thirdColor.bright,
    thirdColorDisabled: thirdColor.dark,

    textColorOnThird: 'black',
    textColorOnPrimary: 'white',
    textColorOnSecondary: 'white',
    textColorOnThird: 'black',
    textColor: 'white',
    primaryfont,
}